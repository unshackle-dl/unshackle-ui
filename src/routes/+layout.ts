// Thin client: all data is fetched in the browser against a user-configured API
// (base URL/key live in localStorage), so render as an SPA.
export const ssr = false;
export const prerender = false;
